<p>Bonjour <%= @user.first_name %>,</p>
<p>Tu as été ajouté dans le fil de discussion #<%= @channel.name %> par <%= @created_by.name %> qui a le sujet suivant : <br>
  <p><%= @channel.description.present? ? @channel.description : @channel.name %></p>
</p>

<p>Tu peux désormais y communiquer avec l(es) autre(s) membre(s) qui ont été ajouté(s).</p>

<table border="0" cellpadding="0" cellspacing="0" width="600" style="width:155px">
  <td>
    <a href= <%= appartments_url(@user.building.slug, @channel.id, token: @user.token) %> style="text-decoration: none;">
      <span style="text-align: center;
        width: 210px;
        font-family: helvetica;
        background-color: #FF6E41;
        padding: 8px;
        color: white;
        margin-left: 16px;
        font-size: 14px;
        display: block;">Me connecter
      </span>
    </a>
  </td>
</table>
